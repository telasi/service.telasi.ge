production:
  sessions:
    default:
      database: telasi_service
      hosts:
        - localhost:27017
      options:
  options:
development:
  sessions:
    default:
      database: telasi_service_development
      hosts:
        - localhost:27017
      options:
  options:
test:
  sessions:
    default:
      database: telasi_service_test
      hosts:
        - localhost:27017
      options:
        consistency: :strong
        max_retries: 1
        retry_interval: 0