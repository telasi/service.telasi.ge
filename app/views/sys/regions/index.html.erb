<div class="page-header">
  <h2>რეგიონები</h2>
</div>
<div class="btn-toolbar">
  <%= link_to icon_text('refresh', 'სინქრონიზაცია'), sys_sync_regions_path, method: :post, class: 'btn', data: {confirm: 'ნამდვილად გინდათ სინქრონიზაცია?'} %>
</div>
<table class="table table-bordered table-striped">
  <thead>
    <tr>
      <th width="50">კოდი</th>
      <th width="250">დასახელება</th>
      <th>მისამართი</th>
      <th>ტელეფონი</th>
    </tr>
  </thead>
  <tbody>
    <% @regions.each do |reg| %>
      <tr>
        <td class="right"><%= reg.regionkey %></td>
        <td><%= link_to reg.name, sys_region_location_url(id: reg.id) %></td>
        <td><%= reg.address %></td>
        <td><%= reg.phone %></td>
      </tr>
    <% end %>
  </tbody>
</table>