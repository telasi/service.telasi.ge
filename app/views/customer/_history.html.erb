<style type="text/css">
  .accnumb { font-size: 0.8em; }
</style>
<div class="page-header">
  <h3>
    <%= @customer.custname.to_ka%>
    <span class="badge badge-warning accnumb">აბ. №<%= @customer.accnumb.to_ka %></span>
    <small><%= @customer.address.to_s %></small>
  </h3>
</div>
<p class="muted">
  დააჭირეთ ოპერაციის დასახელებაზე, დეტალური ინფორმაციის სანახავად.
</p>
<table class="table table-bordered table-striped">
  <thead>
    <tr>
      <th width="100">თარიღი</th>
      <th>ოპერაცია</th>
      <th width="100">ჩვენება (კვ.სთ)</th>
      <th width="100">ხარჯი (კვ.სთ)</th>
      <th width="100">ხარჯი (ლარი)</th>
    </tr>
  </thead>
  <tbody>
    <% @items.each do |item| %>
      <tr>
        <td class="date-field center"><%= C12::KA.format_date(item.itemdate, format: :short) %></td>
        <td class="<%= billoperation_class(item.operation) %>">
          <%= link_to item.operation.billopername.to_ka, history_item_url(accnumb: @customer.accnumb, itemkey: item.itemkey) %>
        </td>
        <td class="right"><%= C12.number_format item.reading, 3 %></td>
        <td class="right"><%= C12.number_format item.kwt, 1 %></td>
        <td class="right"><%= C12.number_format item.amount, 2 %></td>
      </tr>
    <% end %>
  </tbody>
</table>
<div class="paginate-thin">
  <%= paginate @items %>
</div>